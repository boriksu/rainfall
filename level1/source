#include <stdio.h>
#include <stdlib.h>

void run(void) {
	char	s[] = "Good... Wait what?";

	fwrite((void*)s, sizeof(s), 1, 1);
	system("/bin/sh");
	return;
}

int	main() {
	char	str[64];

	gets(str);
	return (0);
}