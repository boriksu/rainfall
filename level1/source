#include <stdio.h>
#include <stdlib.h>

void run()
{
	char	s[] = "Good... Wait what?";

	fwrite((void*)s, sizeof(s), 1, 1);
	system("/bin/sh");
}

int	main()
{
	char	str[80];

	gets(str);
	return (0);
}