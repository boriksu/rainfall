#include <stdio.h>
#include <time.h>
#include <stdlib.h>

char pass[80];

void m()
{
    printf("%s - %d\n", pass, time(0));
}

int main(int argc,char **argv)
{
    char *buf1;
    char *buf2;
    FILE *des;

    str1 = (char *)malloc(8);
    *str1 = 1;

    buf2 = (char *)malloc(8);
    *buf2 = 2;

    strcpy(buf1[1], argv[1]);
    strcpy(buf2[1], argv[2]);

    des = fopen("/home/user/level8/.pass","r");
    fgets(pass, 68, des);
    puts("~~");
    return (0);
}