#include <stdio.h>
#include <stdlib.h>

void	p(char *str)
{
	printf("%s", str);
}

void	n()
{
	char	b[512];
	int	to_over = 0;

	fgets(b, 512, stdin);
	p(b);
	if (to_over == 16930116) {
		system("/bin/cat /home/user/level5/.pass");
	}
}

int		main()
{
	n();
    return (0);
}