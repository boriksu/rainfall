смотрим, где мы оказались и что у нас есть
    ls
    level0

запускам, без аргументов выдает ошибку:
    Segmentation fault (core dumped)
с аргументами тоже:
    No !

посмотрим, что внутри с помощью gdb:
    gdb level0
    disas main

анализируя выполняемые команды, замечаем:
    ...
    0x8048ed4 <main+20>:	call   0x8049710 <atoi>
    0x8048ed9 <main+25>:	cmp    eax,0x1a7
    0x8048ede <main+30>:	jne    0x8048f58 <main+152>
    ...

в данном месте используется atoi для аргумента и сравнивается с числом 0x1a7
что в десятичной системе - 423

после запуска с данным аргументом запускается bash и мы можем посмотреть файл с паролем:
    ./level0 423
    cat /home/user/level1/.pass
